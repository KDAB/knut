int main()
{
    QPoint *somePoint;
    QPixmap *someImage;
    QColor *someColor;
    int *someInt;
    char *someStr;
    short *someShort;
    short *someShortArray;
    bool *someBool;
    ABW_SomeWidget->setCurrentIndex(*someInt);
    someInt = ABW_SomeWidget->currentIndex();
    ABW_SomeWidget->setTextString("HWEOLRLLOD");
    catchall = ABW_SomeWidget->textString();
    ABW_SomeWidget->move(somePoint);
    somePoint = ABW_SomeWidget->pos();
    ABW_SomeWidget->setFlag(Photon::Widget::Ghost, true);
    ABW_SomeWidget->setFlag(Photon::Widget::Blocked, true);
    ABW_SomeWidget->setFlag(Photon::Widget::Ghost, true);
    ABW_SomeWidget->setFlag(Photon::Widget::Blocked, true);
    kdab_setCurrentIndexByTitle(ABW_SomeWidget, "Title");
    someStr = ABW_SomeWidget->tabText(ABW_SomeWidget->currentIndex());
    ABW_SomeWidget->seUserDataPtr<>(somePoint);
    somePoint = ABW_SomeWidget->userDataPtr<0*>();
    ABW_SomeWidget->setSelection(someShortArray);
    someShortArray = ABW_SomeWidget->selection();
    ABW_SomeWidget->setImage(someImage);
    someImage = ABW_SomeWidget->image();
    ABW_SomeWidget->setValue(*someInt);
    someInt = ABW_SomeWidget->value();
    ABW_SomeWidget->setFillColor(Qt::red);
    someColor = ABW_SomeWidget->fillColor();
    ABW_SomeWidget->setColor(QColor(192, 192, 192));
    someColor = ABW_SomeWidget->color();
    ABW_SomeWidget->setLabelType(Photon::Label::Image);
    someInt = ABW_SomeWidget->KDAB_FIX_GETTER_Pt_ARG_LABEL_TYPE() /*N/A for QLabel*/;
    ABW_SomeWidget->setFlag(Photon::Widget::Toggle, true);
    ABW_SomeWidget->setFlag(Photon::Widget::Toggle, false);
    ABW_SomeWidget->setFlag(Photon::Widget::Toggle, true);
    ABW_SomeWidget->setFlag(Photon::Widget::Toggle, false);
    ABW_SomeWidget->setFlag(Photon::Widget::Toggle, true);
    ABW_SomeWidget->setFlag(Photon::Widget::GetsFocus, true);
    ABW_SomeWidget->setFlag(Photon::Widget::GetsFocus, false);
    ABW_SomeWidget->setFlag(Photon::Widget::GetsFocus, *someBool);
    ABW_SomeWidget->setMaximum(10);
    someInt = ABW_SomeWidget->maximum();
    ABW_SomeWidget->setMaximum(20);
    someInt = ABW_SomeWidget->maximum();
    ABW_SomeWidget->setMinimum(5);
    someInt = ABW_SomeWidget->minimum();
    ABW_SomeWidget->setMinimum(1);
    someInt = ABW_SomeWidget->minimum();
    ABW_SomeWidget->setFontStem("somefont");
    someStr = ABW_SomeWidget->font();
    ABW_SomeWidget->setPosition(1);
    someInt = ABW_SomeWidget->position();
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::SingleSelection);
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::MultiSelection);
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::ExtendedSelection);
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::SingleSelection);
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::ContiguousSelection);
    ABW_SomeWidget->setSelectionModeFlags(QAbstractItemView::NoSelection);
    someInt = ABW_SomeWidget->selectionMode();
    ABW_SomeWidget->setMaximum(99);
    someInt = ABW_SomeWidget->maximum();
    ABW_SomeWidget->setMinimum(-4);
    someInt = ABW_SomeWidget->minimum();
    ABW_SomeWidget->setValue(30);
    someInt = ABW_SomeWidget->value();
    ABW_SomeWidget->setSuffix("someSuffix");
    someStr = ABW_SomeWidget->suffix();
    ABW_SomeWidget->setPrefix("somePrefix");
    someStr = ABW_SomeWidget->prefix();
}
