void bah() {
    PtArg_t args[3];
    Photon::Meter *widget = new Photon::Meter(0);
    nargs = 0;
    if (true) {
        widget->setGeometry(&area);
        widget->setKeyLeft(Qt::Key_Down);
        widget->setKeyRight(Qt::Key_Up);
    } else {
        widget->setIncrement(10);
        connect(widget, SIGNAL(KDAB_FIX_Pt_CB_METER_MOVED), &cb[0]) /*KDAB_NOTE: add connection for each member of callback array. Array len: 0*/;
    }
}

void bah2() {
    Photon::Meter *widget;
    PtArg_t args[3];
    widget = new Photon::Meter(0);
    nargs = 0;
    if (true) {
        widget->setGeometry(&area);
        widget->setKeyLeft(Qt::Key_Down);
        widget->setKeyRight(Qt::Key_Up);
    } else {
        widget->setIncrement(10);
        connect(widget, SIGNAL(KDAB_FIX_Pt_CB_METER_MOVED), &cb[0]) /*KDAB_NOTE: add connection for each member of callback array. Array len: 0*/;
    }
}

void bah3() {
    PtArg_t args[3];
    Photon::Meter *widget = new Photon::Meter(0);
    nargs = 0;
    if (true) {
        widget->setGeometry(&area);
        widget->setKeyLeft(Qt::Key_Down);
        widget->setKeyRight(Qt::Key_Up);
    } else {
        widget->setIncrement(10);
        connect(widget, SIGNAL(KDAB_FIX_Pt_CB_METER_MOVED), &cb[0]) /*KDAB_NOTE: add connection for each member of callback array. Array len: 0*/;
    }
}
